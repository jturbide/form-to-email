# ==========================================================
# Form-to-Email Example Environment Configuration
# ==========================================================
# Copy this file to `.env` for production, or `.env.test` for local testing.
# Example:
#   cp .env.example .env
#   cp .env.example .env.test
#
# This file defines configuration for:
#  - SMTP mail delivery
#  - Default sender identity
#  - Recipient list
#  - Optional templates and subject line
# ==========================================================


# ----------------------------------------------------------
# SMTP Configuration
# ----------------------------------------------------------

# The mail server host (e.g., your domain mail server or relay)
SMTP_HOST=mail.example.com

# The SMTP port: use 465 for SSL, 587 for TLS, 25 for plain
SMTP_PORT=465

# SMTP authentication credentials
SMTP_USER=no-reply@example.com
SMTP_PASS=changeme

# Encryption protocol (ssl, tls, or leave empty)
SMTP_ENCRYPTION=ssl

# Whether to use SMTP authentication (true/false)
SMTP_AUTH=true

# Optional PHPMailer debug level (0=off, 2=verbose)
SMTP_DEBUG=0


# ----------------------------------------------------------
# Default Sender (From)
# ----------------------------------------------------------

# Default "From" email address
FROM_EMAIL=no-reply@example.com

# Friendly sender name displayed in the inbox
FROM_NAME="Website Contact Form"


# ----------------------------------------------------------
# Notification Recipients
# ----------------------------------------------------------

# Comma-separated list of destination addresses
# Example: CONTACT_RECIPIENTS="admin@example.com,support@example.com"
CONTACT_RECIPIENTS="contact@example.com"

# Default subject line if not provided by the form
DEFAULT_SUBJECT="New Form Submission"


# ----------------------------------------------------------
# Optional Template Overrides
# ----------------------------------------------------------

# You can define custom email templates using placeholders like {{name}} and {{message}}
# If unset, default templates are automatically used.
# Example:
# CUSTOM_HTML_TEMPLATE=/path/to/custom-email.html
# CUSTOM_TEXT_TEMPLATE=/path/to/custom-email.txt


# ----------------------------------------------------------
# Environment
# ----------------------------------------------------------

# Can be: local, staging, production, test
APP_ENV=local

# Set to "true" to enable debug logging in PHP (e.g., error_log)
APP_DEBUG=true
